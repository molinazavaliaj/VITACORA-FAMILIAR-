# Pase de manos — 20/09/2026 (tarde) → hasta mañana lo corre hermes

Estado escrito para que cualquier agente (hermes/DeepSeek, Joaquín, el Claude de la
PC de música) siga sin preguntar. Todo lo de abajo está en `main` (`1060b81`).
Antes de tocar nada: `git pull`. Estilo y reglas: `supabase/CONTRATO.md`,
`voz/README.md`, `docs/tareas-hermes-2026-09-20.md` (sección "Estilo").

## Dónde está cada cosa (hechos verificados hoy)

- **Fábrica** (`fabrica/`, Railway `fearless-kindness` → `dazzling-friendship`, deploy
  automático desde main): escribe `narracion.json` **v2** para todo pedido con voz
  clonada (historias con audio real + conectores en la voz del narrador, cacheados
  en `conectores_cap_NN.json`); el capítulo clonado se sube tal cual lo masterizó el
  worker; mp3 completo opcional (tope 50 MB); `costos.json` por libro con lo gastado
  en el modelo (`npx tsx scripts/costos.ts <narradorId>`); previsualización con la
  foto de tapa elegida. 289 tests (`cd fabrica && npx vitest run`).
- **Worker de voz** (`voz/`, PC de música, tarea `VitacoraVoz`): masterizado
  (loudnorm −19 LUFS 2 pasadas, EQ al objetivo, limitador), pausas por puntuación
  (`PAUSAS_MS`, modo `oracion`), restauración de originales con `resemble-enhance`
  (`RESTAURACION_NIVEL`, 0,7), ritmo con Whisper (arranques, silencios), anuncio del
  capítulo con la voz clonada. 108 tests (`cd voz && python -m pytest -q`).
  **OJO: el worker de la PC de música todavía corre código viejo** (no hizo `git pull`
  desde el 19/09 a la mañana): las directivas 03, 04 y 05 del buzón siguen sin acuse.
- **Buzón**: bucket `audios` de Supabase. Central → PC de música en `central/*.md`;
  respuestas en `pruebas/<fecha>/`; acuse = `central/<nombre>.leido.txt`. Para leer
  el buzón desde cualquier máquina con el service key: listar
  `POST {SUPABASE_URL}/storage/v1/object/list/audios` con `{"prefix":"central"}`.
- **Web** (Vercel): `AudiolibroPaths.completo` opcional; UI de fotos con
  `posicion`/`foco` (Joaquín, 19/09); marca paraguas "Vitácora" + landing `/viaje`.
- **Pagos**: Argentina con Mercado Pago (funciona). España: Stripe directo, Naza se
  da de alta como autónoma (decidido hoy; el código ya existe, faltan las claves en
  Vercel). Shopify descartado.
- **Libro de Joaquín** (narrador `3691baf4-…`, pedido `3284c93c-…`): `entregado` el
  19/09, todo-clonado (referencia día 02), sin intro. Falta la versión **híbrida**.
- **Keys**: `fabrica-local` (Anthropic, caduca 18/10) en `fabrica/.env` y
  `entrevistador/.env`; `RAILWAY_API_TOKEN` (token de proyecto) en `fabrica/.env`
  para `fabrica/scripts/railway-logs.py` (el CLI de Railway está bloqueado por
  Windows en la PC de Naza). Joaquín tiene que **rotar su key de OpenAI** (quedó
  impresa en un chat el 19/09): key nueva → Railway `VITACORA-FAMILIAR-` →
  `OPENAI_API_KEY`; la fábrica la sigue por referencia.

## Roadmap de HOY, en orden (quién / qué / cómo se verifica)

1. **PC de música — leer el buzón** (Naza le dice "leé el buzón"; ideal
   `/loop 10m` allá). Debe: (a) directiva 03: `git pull` en `C:\vitacora-voz\repo`
   + reiniciar `VitacoraVoz`; (b) directiva 04: **modo híbrido en el worker** contra
   el `narracion.json` v2 (formato en `supabase/CONTRATO.md`, sección "narracion.json
   v2"); (c) subir parches a `pruebas/<fecha>/` y `hibrido-listo.txt`.
   Verificación: aparecen `central/2026-09-20-0{3,4,5}-*.leido.txt`.
2. **hermes — script para disparar la prueba híbrida de Joaquín** (hacerlo ya, se
   usa cuando llegue `hibrido-listo`). `fabrica/scripts/narracion-v2.ts <narradorId>
   <pedidoId>`: baja `{narrador}/paquete/narracion.json` (v1, tiene `texto` por
   capítulo) y `estructura.json`; lee `narradores.edicion` (`leerEdicion`), las
   respuestas y preguntas del narrador (igual que `generar-paquete.ts`); arma las
   historias con `historiasDelCapitulo` y los conectores con `escribirConectores`
   (`fabrica/src/voz/conectores.ts`, usando el caché `conectores_cap_NN.json` si
   existe); escribe `narracion.json` v2 con `armarNarracionJson` (upsert); inserta
   una fila en `narraciones` (`crearNarracion` de `fabrica/src/voz/narraciones.ts`)
   y pone el pedido en `esperando_voz`. Con `--solo-json` no toca la base (para
   mirar el resultado antes). Test con mocks como `generar-paquete.test.ts`. Rama
   `fabrica-script-narracion-v2`, sin merge. **No correrlo contra Joaquín hasta que
   la PC de música diga "híbrido listo"** (si no, narra todo clonado de nuevo: 3 h
   de GPU).
3. **hermes — docs**: `docs/piloto-bitacora-errores.md`: agregar hallazgos **38** (ensamblado
   fallido 18/09 por tope 50 MB + OpenAI sin crédito en la key de la fábrica nueva;
   arreglos `ee3d255`, referencia a la key del entrevistador) y **39** (CLI de
   Railway bloqueado por Control de aplicaciones de Windows; `scripts/railway-logs.py`
   con token de proyecto). Solo con datos de `GASTOS.md`, `ESTADO.md` y los commits.
4. **Joaquín**: rotar la key de OpenAI (arriba); probar la UI de fotos con una foto
   **vertical** en posición `abajo` y mirar el PDF; leer `ESTADO.md` y `CONTRATO.md`
   (secciones nuevas de hoy); cuando Naza tenga Stripe: cargar `STRIPE_SECRET_KEY`
   y `STRIPE_WEBHOOK_SECRET` en Vercel y apuntar el webhook.
5. **Naza**: trámite de autónoma + Stripe; a la noche, capturas de Usage de
   Anthropic (18 y 19 sept UTC) suya y de Joaquín para cerrar `GASTOS.md` (hasta
   ahora estimado: libro de Joaquín ~USD 10; hoy la fábrica ya mide sola con
   `costos.json`, así que esto es solo para cerrar lo pasado).

## Lo que NO hay que hacer
- No marcar pedidos `pagado` a mano si el libro no está cerrado (`libro_aprobado_at`).
- No imprimir valores de keys/tokens al verificar variables (Railway resuelve las
  referencias `${{…}}` al valor real). Comparar con `==`, mostrar solo igual/distinta.
- No tocar `voz/` desde la central salvo revisión de parches: es de la PC de música.
- No borrar `conectores_cap_NN.json`, `borrador_*` ni `narracion.json` a mano.

## Cuando llegue "híbrido listo" (mañana, probablemente)
1. Correr el script del punto 2 contra Joaquín (`--solo-json` primero; leer los
   conectores generados; si suenan bien, sin `--solo-json`).
2. El worker narra (restaura originales, conectores clonados, masteriza): ~30–40
   min de GPU en vez de 3 h. La fábrica ensambla y entrega; el panel reproduce.
3. Naza escucha el libro entero híbrido contra el todo-clonado y decide. Si va:
   directiva 06 = híbrido por defecto para todo pedido con `clonada` (ya lo es del
   lado de la fábrica; falta que el worker lo tenga en producción).
