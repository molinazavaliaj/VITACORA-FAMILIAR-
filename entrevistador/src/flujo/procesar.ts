import type { MensajeEntrante } from '../whatsapp/webhook.js';

// Stub: la lógica completa (consentimiento, respuestas, repregunta, pausa) llega en la Task 7.
export async function procesarEntrante(_m: MensajeEntrante): Promise<void> {}
